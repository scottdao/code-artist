<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.code.artist</groupId>
    <artifactId>code-artist-parent</artifactId>
    <packaging>pom</packaging>
    <version>1.0-SNAPSHOT</version>
    <name>code-artist-parent</name>
    <properties>
        <!-- common projects -->
        <code-artist-common.version>1.0-SNAPSHOT</code-artist-common.version>
        <code-artist-utils.version>1.0-SNAPSHOT</code-artist-utils.version>
        <!-- facade projects -->
        <code-artist-facade.version>1.0-SNAPSHOT</code-artist-facade.version>
        <code-artist-facade-portal.version>1.0-SNAPSHOT</code-artist-facade-portal.version>
        <!-- service projects -->
        <code-artist-service.version>1.0-SNAPSHOT</code-artist-service.version>
        <code-artist-service-portal.version>1.0-SNAPSHOT</code-artist-service-portal.version>
        <!-- rest projects-->
        <code-artist-cms.version>1.0-SNAPSHOT</code-artist-cms.version>
        <code-artist-portal.version>1.0-SNAPSHOT</code-artist-portal.version>
        <!-- frameworks -->
    </properties>

    <dependencyManagement>
        <dependencies>

        </dependencies>
    </dependencyManagement>

    <profiles>
        <profile>
            <!-- 本地开发环境 -->
            <id>dev</id>
            <properties>
                <profiles.active>dev</profiles.active>
            </properties>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
        </profile>
        <profile>
            <!-- 生产环境 -->
            <id>prod</id>
            <properties>
                <profiles.active>prod</profiles.active>
            </properties>
        </profile>
    </profiles>
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.7.0</version>
                <configuration>
                    <failOnError>true</failOnError>
                    <verbose>true</verbose>
                    <fork>true</fork>
                    <compilerArgument>-nowarn</compilerArgument>
                    <source>1.8</source>
                    <target>1.8</target>
                    <encoding>UTF-8</encoding>
                    <encoding>UTF-8</encoding>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>